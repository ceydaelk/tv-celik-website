---
phase: 01-shell-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - src/components/common/WhatsAppFAB.tsx
autonomous: true
requirements:
  - UI-01
  - CTA-01

must_haves:
  truths:
    - "app/globals.css contains brand color tokens inside the @theme inline block"
    - "app/layout.tsx loads Inter font (not Geist) via next/font/google with latin and latin-ext subsets"
    - "app/layout.tsx sets lang=\"tr\" on the <html> element"
    - "WhatsAppFAB component renders a fixed-position green circle in the bottom-right corner on every page"
    - "app/page.tsx returns a minimal placeholder div with no scaffold content"
  artifacts:
    - path: "src/components/common/WhatsAppFAB.tsx"
      provides: "Persistent WhatsApp floating action button"
      exports: ["WhatsAppFAB (default)"]
    - path: "app/globals.css"
      provides: "Tailwind CSS 4 brand tokens"
      contains: "@theme inline"
    - path: "app/layout.tsx"
      provides: "Root layout with Inter font, lang=tr, and WhatsAppFAB wired in"
      contains: "WhatsAppFAB"
  key_links:
    - from: "app/layout.tsx"
      to: "src/components/common/WhatsAppFAB.tsx"
      via: "import and render inside <body>"
      pattern: "import WhatsAppFAB"
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "--font-inter CSS variable consumed by @theme inline --font-sans"
      pattern: "--font-inter"
---

<objective>
Configure the project foundation: brand tokens in Tailwind CSS 4, Inter font replacing Geist, Turkish lang attribute, lucide-react dependency, WhatsAppFAB component, and a clean placeholder home page.

Purpose: Every subsequent phase depends on this shell. Brand colors, correct font, and the persistent WhatsApp FAB must be in place before any page content is built.

Output: Configured globals.css, updated layout.tsx, WhatsAppFAB component, minimal page.tsx placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current state of files this plan modifies. Read before touching anything. -->

From app/layout.tsx (current — REPLACE entirely):
```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

From app/globals.css (current — REPLACE @theme block):
```css
@import "tailwindcss";
:root { --background: #ffffff; --foreground: #171717; }
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}
@media (prefers-color-scheme: dark) { :root { ... } }
body { background: var(--background); color: var(--foreground); font-family: Arial, Helvetica, sans-serif; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind brand tokens and Inter font</name>
  <files>app/globals.css, app/layout.tsx</files>
  <read_first>
    - app/globals.css — read the full current file before editing
    - app/layout.tsx — read the full current file before editing
    - .planning/phases/01-shell-navigation/01-UI-SPEC.md — §Tailwind CSS 4 Token Declarations, §Typography
    - .planning/phases/01-shell-navigation/01-CONTEXT.md — §Decisions D-08 through D-11
  </read_first>
  <action>
**Step 1 — Install lucide-react:**
```
npm install lucide-react
```

**Step 2 — Rewrite app/globals.css:**

Replace the entire file content with:

```css
@import "tailwindcss";

@theme inline {
  /* Brand colors — from UI-SPEC.md §Color and §Tailwind CSS 4 Token Declarations */
  --color-navy:           #0f172a;
  --color-accent:         #2563eb;
  --color-accent-hover:   #1d4ed8;
  --color-whatsapp:       #25D366;
  --color-whatsapp-hover: #1da851;
  --color-surface-tint:   #f0f7ff;
  --color-muted:          #64748b;
  --color-border:         #e2e8f0;

  /* Typography */
  --font-sans: var(--font-inter);
}

body {
  background: #ffffff;
  color: #0f172a;
  font-family: var(--font-sans), system-ui, sans-serif;
}
```

Remove the dark mode `:root` block and the `:root` block entirely. Do NOT keep `--color-background` or `--color-foreground` — those are scaffold defaults.

**Step 3 — Rewrite app/layout.tsx:**

Replace the entire file content with:

```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { WhatsAppFAB } from "@/components/common/WhatsAppFAB";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin", "latin-ext"],
  weight: ["400", "700"],
});

export const metadata: Metadata = {
  title: "TV Çelik A.Ş.",
  description: "Prefabrik, hafif çelik ve konteyner yapı sistemleri",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="tr" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        {children}
        <WhatsAppFAB />
      </body>
    </html>
  );
}
```

Notes:
- Geist and Geist_Mono imports are removed entirely
- `lang="en"` becomes `lang="tr"` (per D-04 locale requirement)
- `inter.variable` exposes `--font-inter` which globals.css picks up via `--font-sans: var(--font-inter)`
- WhatsAppFAB is placed after `{children}` inside `<body>` so it stacks above all page content
- Navbar and Footer are NOT wired here yet — those come in Plan 01-02
  </action>
  <verify>
    <automated>
      Check Inter is imported (not Geist): grep -c "from \"next/font/google\"" app/layout.tsx
      Check lang is tr: grep -c "lang=\"tr\"" app/layout.tsx
      Check --font-inter variable: grep -c "\-\-font-inter" app/globals.css
      Check brand color navy in globals: grep -c "\-\-color-navy" app/globals.css
      Check whatsapp color in globals: grep -c "\-\-color-whatsapp" app/globals.css
      Check no Geist imports remain: grep -c "Geist" app/layout.tsx (must return 0)
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` contains `import { Inter } from "next/font/google"` (exact string)
    - `app/layout.tsx` contains `lang="tr"` (exact string)
    - `app/layout.tsx` contains `subsets: ["latin", "latin-ext"]` (exact string)
    - `app/layout.tsx` contains `variable: "--font-inter"` (exact string)
    - `app/layout.tsx` does NOT contain the string "Geist"
    - `app/globals.css` contains `--color-navy:` with value `#0f172a`
    - `app/globals.css` contains `--color-accent:` with value `#2563eb`
    - `app/globals.css` contains `--color-whatsapp:` with value `#25D366`
    - `app/globals.css` contains `--font-sans: var(--font-inter)`
    - `app/globals.css` does NOT contain `--font-geist-sans`
    - `node_modules/lucide-react` directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>Inter font loaded with latin + latin-ext subsets; brand tokens in @theme inline; lang="tr" set; Geist fully removed; lucide-react installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create WhatsAppFAB component and clear page.tsx scaffold</name>
  <files>src/components/common/WhatsAppFAB.tsx, app/page.tsx</files>
  <read_first>
    - .planning/phases/01-shell-navigation/01-UI-SPEC.md — §Component Specifications §5 WhatsAppFAB (full spec)
    - app/layout.tsx — confirm import path matches what was written in Task 1
  </read_first>
  <action>
**Step 1 — Create directory structure:**
Create `src/components/common/` directory (mkdir -p or equivalent).

**Step 2 — Create src/components/common/WhatsAppFAB.tsx:**

```typescript
"use client";

import { MessageCircle } from "lucide-react";

export function WhatsAppFAB() {
  return (
    <div className="group fixed bottom-6 right-6 z-50">
      {/* Tooltip — visible on desktop hover */}
      <span
        className="absolute right-16 top-1/2 -translate-y-1/2 whitespace-nowrap rounded bg-[#0f172a] px-2 py-1 text-xs text-white opacity-0 transition-opacity group-hover:opacity-100"
        aria-hidden="true"
      >
        Hemen Yazın
      </span>

      {/* FAB button */}
      <a
        href="https://wa.me/90XXXXXXXXXX?text=Merhaba%2C%20bilgi%20almak%20istiyorum."
        target="_blank"
        rel="noopener noreferrer"
        aria-label="WhatsApp ile İletişime Geçin"
        className="flex h-14 w-14 items-center justify-center rounded-full bg-[#25D366] shadow-lg transition-all duration-150 hover:bg-[#1da851] hover:shadow-xl hover:scale-105 active:scale-95 focus:outline-2 focus:outline-[#25D366]"
      >
        <MessageCircle size={28} className="text-white" />
      </a>
    </div>
  );
}
```

Notes:
- `"use client"` directive required because this component uses CSS group-hover interaction (client rendering needed for tooltip; component has no server-side data needs)
- href uses placeholder phone number `90XXXXXXXXXX` — the real number is not available in source artifacts; executor leaves placeholder
- `group` class is on the wrapper `<div>`, `group-hover:opacity-100` on the tooltip — this is pure CSS, no JS state
- Component exports as named export `WhatsAppFAB` (matches import in layout.tsx)

**Step 3 — Replace app/page.tsx with minimal placeholder:**

```typescript
export default function Home() {
  return <div />;
}
```

This clears all Next.js scaffold content. Phase 2 builds the real home page content.
  </action>
  <verify>
    <automated>
      Check component file exists: test -f src/components/common/WhatsAppFAB.tsx
      Check named export: grep -c "export function WhatsAppFAB" src/components/common/WhatsAppFAB.tsx
      Check aria-label: grep -c "WhatsApp ile İletişime Geçin" src/components/common/WhatsAppFAB.tsx
      Check fixed positioning wrapper: grep -c "fixed bottom-6 right-6 z-50" src/components/common/WhatsAppFAB.tsx
      Check wa.me href: grep -c "wa.me/90" src/components/common/WhatsAppFAB.tsx
      Check page.tsx is minimal: wc -l app/page.tsx (must be ≤ 5 lines)
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/common/WhatsAppFAB.tsx` exists
    - File contains `export function WhatsAppFAB()` (named export, not default)
    - File contains `"use client"` at top
    - File contains `fixed bottom-6 right-6 z-50` (exact wrapper positioning classes)
    - File contains `h-14 w-14 rounded-full` (exact FAB size classes)
    - File contains `bg-[#25D366]` (exact WhatsApp green)
    - File contains `hover:bg-[#1da851]` (exact hover color)
    - File contains `aria-label="WhatsApp ile İletişime Geçin"` (exact Turkish string)
    - File contains `target="_blank" rel="noopener noreferrer"` (exact security attributes)
    - File contains `MessageCircle` imported from `lucide-react`
    - `app/page.tsx` contains no Next.js scaffold content (no Image imports, no Vercel links)
    - `app/layout.tsx` import `{ WhatsAppFAB }` from `@/components/common/WhatsAppFAB` resolves (path matches file)
  </acceptance_criteria>
  <done>WhatsAppFAB renders as a fixed green circle in bottom-right corner with tooltip on hover, correct aria-label, and pre-filled WhatsApp deep link. page.tsx is a clean placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → external URL | User clicks WhatsApp FAB/button; browser navigates to wa.me (external) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | WhatsAppFAB href | accept | Static string in source code, no user input, no dynamic construction |
| T-01-02 | Information Disclosure | `target="_blank"` on wa.me link | mitigate | `rel="noopener noreferrer"` already specified in component — prevents opener access and referrer leakage |
</threat_model>

<verification>
Run the dev server and verify:
1. `npm run dev` starts without TypeScript errors
2. Visit http://localhost:3000 — page renders without errors
3. Inspect `<html>` element — `lang` attribute is `tr`
4. Font in browser DevTools shows Inter (not Geist)
5. WhatsApp FAB (green circle) is visible fixed bottom-right
6. FAB hover shows "Hemen Yazın" tooltip to the left
7. FAB click navigates to wa.me URL in new tab
</verification>

<success_criteria>
- `npm run dev` exits with no TypeScript compilation errors
- `app/globals.css` @theme inline block contains all 8 brand color tokens plus --font-sans
- `app/layout.tsx` uses Inter with latin + latin-ext, sets lang="tr", mounts WhatsAppFAB
- `src/components/common/WhatsAppFAB.tsx` exists with correct fixed positioning, green colors, aria-label, and wa.me href
- `app/page.tsx` is a clean placeholder (no scaffold content)
- `node_modules/lucide-react` installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-navigation/01-01-SUMMARY.md`
</output>
